{
    "name": "lttr",
    "version": "0.0.1",
    "description": "Replacement for Postman",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "start": "ts-node src/run.ts",
        "serve": "ts-node src/run.ts",
        "prestart": "npm run build",
        "test": "jest",
        "clean": "rm -rf junit.xml dist coverage"
    },
    "dependencies": {
        "axios": "^1.6.3",
        "minimist": "^1.2.8",
        "table": "^6.8.1",
        "yaml": "^2.3.4"
    },
    "devDependencies": {
        "ts-node": "^10.9.2",
        "@types/node": "^20.10.6"
    }
}
