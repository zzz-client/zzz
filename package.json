{
    "name": "lttr",
    "version": "0.0.1",
    "description": "Replacement for Postman",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "start": "node dist/src/run.js",
        "prestart": "npm run build",
        "test": "jest",
        "clean": "rm -rf junit.xml dist coverage"
    },
    "dependencies": {
        "axios": "^1.6.3",
        "minimist": "^1.2.8",
        "yaml": "^2.3.4"
    },
    "devDependencies": {
        "@types/node": "^20.10.6"
    }
}
